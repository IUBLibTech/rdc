<% if f.object.multiple? key %>
  <%= f.input key, as: :multi_value, input_html: { class: 'form-control' }, required: f.object.required?(key), hint: hint_with_links(key) %>
<% else %>
  <%= f.input key, required: f.object.required?(key), hint: hint_with_links(key) %>
<% end %>
